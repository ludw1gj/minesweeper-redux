{
  "name": "minesweeper-redux",
  "version": "0.8.8",
  "description": "A minesweeper game implementation written in a functional style supporting the use of redux via the provided actions and reducer.",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch & nodemon dist",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jestconfig.json",
    "prepare": "npm run build",
    "prepublishOnly": "npm test && npm run lint",
    "preversion": "npm run lint",
    "version": "npm run format && git add -A src",
    "postversion": "git push && git push --tags"
  },
  "author": "ludw1gj",
  "license": "ISC",
  "keywords": [
    "Minesweeper",
    "Functional",
    "Redux",
    "Typescript",
    "Game"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ludw1gj/minesweeper-redux.git"
  },
  "devDependencies": {
    "@types/jest": "^23.3.14",
    "jest": "^23.6.0",
    "prettier": "^1.16.4",
    "ts-jest": "^23.10.5",
    "tslint": "^5.12.1",
    "tslint-config-prettier": "^1.18.0",
    "tslint-no-circular-imports": "^0.6.1",
    "typescript": "^3.3.3"
  },
  "files": [
    "lib/**/*"
  ]
}
